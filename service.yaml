apiVersion: v1
kind: Service
metadata:
  name: aceest-gym-service
spec:
  type: NodePort
  selector:
    app: aceest-gym
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007